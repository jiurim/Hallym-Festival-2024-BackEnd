spring:
  datasource:
    url: jdbc:mysql://hallym-festival-2024.c4fwadtpqr2i.ap-northeast-2.rds.amazonaws.com:3306/hallym-festival-2024
    username: ${SPRING_DATABASE_USERNAME}
    password: ${SPRING_DATABASE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      database-platform: org.hibernate.dialect.MySQLDialect
      hibernate:
        ddl-auto: update
        show-sql: true


cloud:
  aws:
    credentials:
      access-key: ${S3_ACCESS_KEY}
      secret-key: ${S3_SECRET_KEY}
    region:
      static: ${AWS_REGION}
    stack:
      auto: false
    s3:
      bucket: ${S3_BUCKET}